{% extends 'subLayout.html.twig' %}

{% block title %}PEMINJAMAN{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="card">
                    <div class="header">
                        <h2>
                            <center>Detail Data Peminjaman</center>
                        </h2>
                    </div>
                    <div class="body">
                        <p>Nama Peminjam : {% if data.namaPeminjam is defined and not null %}{{ data.namaPeminjam }}{% endif %}</p>
                        <p>Petugas Rekam Medis : {% if data.petugasRekamMedis is defined and not null %}{{ data.petugasRekamMedis }}{% endif %}</p>
                        <p>Nama Pengembalian : {% if data.namaPengembalian is defined and not null %}{{ data.namaPengembalian }}{% endif %}</p>
                        <p>Unit Peminjam : {% if data.unitPeminjam is defined and not null %}{{ data.unitPeminjam }}{% endif %}</p>
                        <p>Unit Pengembalian : {% if data.unitPengembalian is defined and not null %}{{ data.unitPengembalian }}{% endif %}</p>
                        <p>Tanggal Peminjam : {% if data.tglPeminjaman is defined and not null %}{{ data.tglPeminjaman|date('d m Y') }}{% endif %}</p>
                        <p>Tanggal Pengembalian : {% if data.tglKembali is defined and not null %}{{ data.tglKembali|date('d m Y') }}{% endif %}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}